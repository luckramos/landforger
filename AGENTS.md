# AGENTS.md

Agent and contributor instructions for this project live in **[CLAUDE.md](./CLAUDE.md)** — read it before making changes.

It covers the monorepo layout (`web/` frontend, `api/` backend), the commands (run frontend scripts from `web/`), coding conventions, and the ratified architecture/design decisions in `docs/adr/`. The HTTP contract shared by both sides is `openapi.yaml` at the repo root.
