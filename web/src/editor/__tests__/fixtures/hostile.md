This paragraph contains \[\[not a link\]\] which must stay literal text.

A line with [mention id="x"] as plain text, not a chip.

| Name | Role |
| ---- | ---- |
| Sera | Captain |

```js
const trap = "[[duskwater]] inside a fence";
```

The end.
